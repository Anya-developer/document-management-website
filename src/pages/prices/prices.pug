extends ../../layout/layout

block variables
  -
    const pageName = 'prices'
    const pageTitle = 'prices'
    const linksObj = [["Главная", "main"]]

block content
  main(data-barba='container' data-barba-namespace=pageName)
    +breadcrumb(linksObj, "Название статьи")
    +tarifs
    +questions-form
